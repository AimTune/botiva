{
    "name": "botiva-monorepo",
    "private": true,
    "type": "module",
    "packageManager": "pnpm@11.5.2",
    "scripts": {
        "build": "tsc -b",
        "clean": "tsc -b --clean",
        "smoke": "pnpm build && tsx examples/ts/smoke-test.ts",
        "demo": "pnpm build && tsx examples/ts/demo-server.ts",
        "demo:graph": "pnpm build && tsx examples/ts/langgraph-server.ts",
        "demo:mcp": "pnpm build && tsx examples/ts/mcp-demo-server.ts"
    },
    "devDependencies": {
        "@types/node": "^22.10.0",
        "tsx": "^4.19.2",
        "typescript": "^5.7.2"
    },
    "engines": {
        "node": ">=18.17"
    }
}
